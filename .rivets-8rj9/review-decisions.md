# PR 102 review decisions

Source: `pr-102-review-findings.md` (`/code-review xhigh PR 102`, 2026-08-30)

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| F1 | Removed `Blocked` cannot decode legacy JSONL and turns a skipped record into a workspace-wide write refusal. | xhigh review | Verified | `IssueRecord.status: IssueStatus` deserializes before `into_domain`; `"blocked"` becomes `MalformedJson`, and `unsafe_partial_load` rejects every write. | Modify | Decode a persistence-only legacy Blocked value as Open, append a migration Note, emit a safe typed warning, and fence canonical/idempotent resave. | Keep Blocked out of the domain; repair only at the compatibility seam. |
| F2 | MCP `update` silently discards removed `assignee` input and accepts empty updates. | xhigh review | Verified | `UpdateParams` has neither `assignee` nor unknown-field rejection; `Tools::update` constructs an all-`None` `IssueUpdate`, and storage advances `updated_at`. | Modify | Add schema-hidden legacy-assignee presence detection and reject it before locking; reject all empty MCP updates before mutation. | `deny_unknown_fields` is incompatible with the intentional flattened Kind compatibility seam. |
| F3 | `CLAUDE.md` instructs an unassigned Ready Issue to enter In Progress without first being claimed. | xhigh review | Verified | `CLAUDE.md:43-53` omits Claim; default Ready is unassigned and `apply_status_transition` rejects unassigned In Progress. | Accept | Change the workflow to Claim first, then move In Progress, then close. | Matches AGENTS.md and `docs/agents/issue-tracker.md`. |
| F4 | Interactive CLI prompts run while the durable Workspace lock is held. | xhigh review | Verified | `Cli::execute` constructs mutation `App` before `execute_create`/confirmations; `App` owns the fail-fast lock until drop. | Modify | Resolve title and confirmations before mutation-App construction; load read-only delete display data before prompting, then acquire/reload and re-fetch for mutation. | The lock still spans authoritative load, mutation, and save—only human think-time leaves the critical section. |
| F5 | Read-only JSONL opens spuriously return `ExternalChange` across a concurrent atomic rename. | xhigh review | Verified | `create_storage` hashes before/load/after and errors on mismatch although the path-based load saw one complete renamed inode snapshot. | Modify | Accept the loaded snapshot, retain the pre-load revision as the stale marker, and rely on `prepare_mutation` plus save guards before any write. | Mutation safety remains; the read path stops applying a write-only TOCTOU fence. |
| F6 | Concurrent mutations in one MCP server acquire the OS lock before the in-process write guard and can fail `WorkspaceBusy` instead of serializing. | xhigh review | Verified | `mutation_storage_for` calls `try_acquire` before `write_owned`; separate file descriptions contend even inside one process. | Accept | Resolve storage and await its write guard first, then acquire the durable lock; strengthen the concurrent MCP test to require both operations succeed. | External writers remain fail-fast; same-server calls serialize locally. |
| F7 | `reopen` accepts In Progress even though its public contract is Closed-only. | xhigh review | Verified | CLI/MCP descriptions say “closed”; adapter tests intentionally replaced the prior NotClosed case, while the generic domain transition allows InProgress→Open. | Modify | Add a shared Closed-only reopen precondition at the adapter/storage transaction seam while preserving generic InProgress→Open update behavior. | Restores the command/tool contract without removing the approved generic transition. |
| F8 | Related/Discovery add/remove skip `prepare_mutation`. | xhigh review | Verified | All four methods in `JsonlBackedStorage` call only `ensure_writable`, unlike every other mutator. | Accept | Route all four through `prepare_mutation` and add stale-source relationship fences. | Required before removing MCP’s unconditional reload. |
| F9 | Empty or whitespace-only Assignee values create a phantom Assignment. | xhigh review | Verified | Assignment methods accept raw `&str`; issue validation rejects controls but not empty text, so `Some("")` is neither unassigned nor meaningfully owned. | Modify | Reject blank Assignee inputs in create/Claim/Release with typed errors; visibly migrate legacy blank persisted Assignment to unassigned. | Supersedes the original design’s permissive empty-text row; requester required every founded defect fixed in this PR. |
| F10 | MCP Claim description overstates idempotency after an Issue leaves Open. | xhigh review | Verified | Storage checks Workflow State before same-owner identity; the tool description says repeated Claim is unqualifiedly idempotent. | Accept | Qualify docs: same-owner retry is idempotent only while the Issue remains Open. | Storage behavior is intentional and already fenced. |
| F11 | Existing Workspaces create a trackable `workspace.lock` because only new `init` writes its ignore rule. | xhigh review | Verified | `WorkspaceMutationLock::try_acquire` creates the sidecar; pre-PR `.rivets/.gitignore` lacks `workspace.lock`. | Modify | Under first acquired mutation lock, idempotently add the sidecar rule to `.rivets/.gitignore`; test a pre-upgrade metadata directory. | Keeps one stable sidecar identity and upgrades existing Workspaces automatically. |
| F12 | MCP unconditionally reloads cached JSONL before every mutation, duplicating revision checks and full-file work. | xhigh review | Verified | `mutation_storage_for` always calls `reload`; `JsonlBackedStorage::prepare_mutation` already reloads iff changed, except the F8 omissions. | Accept | Fix F8, remove unconditional reload, and retain stale-cache/mixed-adapter fences. | Hot path becomes revision hashes plus one save, not an unconditional parse/graph rebuild. |
| F13 | `MockStorage::claim` and `release` panic instead of returning the documented typed unsupported error. | xhigh review | Verified | Both methods use `unimplemented!()` while the public mock contract says stateful mutations return `UnsupportedOperation`. | Accept | Return the same typed unsupported error as other mock mutators and add direct trait tests. | Public test-util code must not abort downstream test processes. |
| F14 | Parity registry still documents removed legacy Blocked status and counts. | xhigh review | Verified | JSON rows 112/203/218/230/984/1026/1053/1071 contradict canonical parser/status output; generated Markdown faithfully repeats them. | Accept | Correct the authoritative JSON and regenerate Markdown; retain registry/status fences. | Documentation synchronization is part of the PR contract. |
| F15 | Workspace lock integration test can steal the child’s fail-fast lock acquisition and bypasses the macOS retry helper. | xhigh review | Verified | The parent repeatedly acquires/releases the same lock while the child gets one attempt; local `run` invokes the binary without `common::run_rivets_in_dir`. | Modify | Replace polling with prompt/pipe handshakes and RAII child cleanup; route ordinary invocations through the common retry helper. | The replacement also permanently fences F4. |
| F16 | PR-added tests contain widespread bare `.unwrap()` contrary to repository test diagnostics guidance. | xhigh review | Verified | Diff inspection finds bare unwraps across new MCP, storage, CLI, and lock tests; `CLAUDE.md:426-435` requires descriptive `expect`. | Accept | Replace every bare unwrap introduced by this PR with a descriptive `expect`; leave deliberate non-PR legacy code untouched. | Mechanical test-only cleanup, verified by diff audit plus compilation. |
| F17 | SHA-256 revision logic is duplicated. | xhigh review | Verified | `SourceRevision::read` and `save_to_jsonl_with_revision` independently instantiate, update, and finalize SHA-256 over the same canonical file-byte domain. | Modify | Introduce one private revision-hasher type used by both existing-file reads and canonical save output without adding another read or allocation. | The I/O loops remain purpose-specific; only revision algorithm ownership is unified. |
| F18 | Generic-list and Ready filter matchers duplicate priority/Kind/label predicates. | xhigh review | Verified | `matches_filter` and `matches_ready_filter` repeat the same three predicate blocks and differ only in status/Assignment-specific eligibility. | Accept | Extract one private common-field matcher and retain distinct status/Assignment predicates. | Small internal cleanup; existing query truth tables fence semantics. |
| F19 | `Commands::mutates_workspace` is a drift-prone parallel table. | xhigh review | Verified | The top-level match is exhaustive, but nested `matches!` expressions contain implicit wildcard-false arms, so a new action variant silently becomes read-only; the parser-string test is separately maintained. | Modify | Give every nested action enum an exhaustive `mutates_workspace` method, delegate from `Commands`, and keep parser tests as representative syntax smoke rather than the ownership oracle. | Adding any top-level or nested action variant must fail compilation until its lock classification is explicit. |
| F20 | Test-only Workspace eviction leaves stale entries in `test_workspaces`. | xhigh review | Verified | `evict_oldest_except` removes `storage_cache` and `database_paths` but not the `#[cfg(test)]` set consulted by mutation-lock bypass. | Accept | Remove the evicted path from `test_workspaces` and assert full metadata cleanup. | Test-only but can misclassify later reused paths and grows without bound. |
| F21 | Production-scale 10k/50k timing fixtures run in every default test gate. | xhigh review | Verified | New graph and Ready stress tests are unignored, unlike existing scale checkpoints; default CI runs the full workspace. | Accept | Mark the two production-scale timing checkpoints ignored with explicit reasons; retain small semantic fences and run exact ignored checks in review verification. | Removes scheduler-sensitive cost from every commit while keeping the named performance gate runnable. |
| F22 | Claim/Release adapter transaction scaffolding is duplicated in CLI and MCP. | xhigh review | Verified | Both pairs duplicate acquire/mutate/save/render structure with only the storage operation and action label differing. | Modify | Introduce small private Assignment-operation helpers/enums per adapter; keep domain checks in storage and user/protocol rendering at each adapter. | Prevents future lock/save behavior drifting between Claim and Release without creating a public abstraction. |
| F23 | CLI tracing setup calls `.with_writer(stderr)` twice. | xhigh review | Verified | `crates/rivets/src/main.rs:21` and `:23` are identical builder calls. | Accept | Remove the duplicate call. | Pure dead configuration. |
| F24 | New MCP mutation locking performs blocking filesystem operations directly in async handlers. | xhigh review | Verified | Async `mutation_storage_for` calls synchronous canonicalize/open/flock through `WorkspaceMutationLock::try_acquire`; explicit path resolution also performs synchronous metadata operations. | Modify | Add an async acquisition path that wraps the full sidecar canonicalize/open/flock operation in `spawn_blocking`, use asynchronous path metadata/canonicalization at MCP boundaries, and avoid duplicate canonicalization. | Preserve the synchronous API for non-async library/CLI callers where appropriate; fence current-thread responsiveness and lock semantics. |

## Gate

Every finding has one evidence state and one decision. No behavior-changing fix rests on an Unverified claim. All 24 findings are accepted or modified for this PR with no deferral.
