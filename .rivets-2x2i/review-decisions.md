# Review decisions

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| F1 | The C0 named mutation widens graph visibility before expecting Rust privacy to reject the CLI bypass; preserve the privacy boundary and make the compile-fail experiment explicit. | OMP code review for PR #99 | Verified | A temporary `use crate::storage::in_memory::inner::InMemoryStorageInner;` in `cli/execute.rs` made `cargo check -p rivets` fail with E0603 because `inner` is private. The existing design/plan instead instructed the mutation to widen visibility first. | Accept | Correct the C0 design and Slice 2 named mutation so the deliberate CLI access leaves visibility unchanged, expects E0603 from `cargo check -p rivets`, and restores to a green check. | Non-behavioral verification repair; no checkpoint or tracker issue required. |
| F2 | `MockStorage::import_issues`, `save`, and `reload` changed from successful no-ops to unsupported errors outside the relationship scope. | Claude reviews on PR #99 (comments 1 and 3) | Verified | The PR diff replaces each existing `Ok(())` with `StorageError::UnsupportedOperation`; the methods are public under `test-util`. | Accept | Review-fix Slice R1 restores the three existing no-op contracts while retaining typed unsupported errors for stateful mutations. | Slice R1 gate PASS: the fence was RED under the unsupported behavior and GREEN after restoration; final integration passed 1,106 tests with 1 ignored. |
| F3 | Canonical-left Related ownership and reverse legacy lookup need additional inline comments. | Claude reviews on PR #99 (comments 1 and 3) | Not-applicable | N/A — documentation preference; the implementation already labels the compatibility owner, while the approved design and restart test state canonical-left writes and legacy reverse reads. | Reject | N/A — existing documentation names the invariant at its owning design and test seams. | No correctness or ambiguity remains at the interface. |
| F4 | One-variant domain relationship errors may not distinguish future adapter failures coherently. | Claude review on PR #99 (comment 1) | Refuted | Domain errors reject invalid relationship values; storage errors carry not-found, duplicate, and cycle outcomes. Stacked PR #100 maps those storage outcomes at the adapter boundary and tests their MCP classification. | Reject | N/A — the existing error split follows the domain/storage seam. | No current failure or missing variant was identified. |
| F5 | A self-referential persisted Related record bypasses the domain invariant and is inserted silently by the JSONL loader. | Claude review on PR #99 (comment 2) | Verified | `jsonl.rs` maps `DependencyType::Related` to `creates_cycle = false` and then unconditionally calls `add_edge`, even when the record owner equals its target. | Accept | Review-fix Slice R1 rejects self-reference before kind-scoped cycle evaluation, records a warning, skips the edge, and adds a resilient-loading regression fence. | Slice R1 gate PASS: the fence was RED with zero warnings before the guard and GREEN after restoration; final integration passed 1,106 tests with 1 ignored. |
| F6 | Related and Discovery removals duplicate descending `EdgeIndex` removal logic. | Claude review on PR #99 (comment 2) | Verified | Both methods collect matching edges, sort descending, and remove them. | Reject | N/A — two explicit call sites keep their distinct persistence cleanup visible; a pass-through helper would not deepen the module or change behavior. | Pure refactor suggestion, not a defect. |
| F7 | Defensive relationship query branches warn and skip invalid graph edges without a dedicated branch test. | Claude review on PR #99 (comment 3) | Not-applicable | N/A — the comment proposes optional test/assertion policy rather than identifying incorrect observable behavior. | Reject | N/A — resilient warning behavior remains preferable to a production panic; F5 prevents the identified persisted self-loop from entering the graph. | Defense-in-depth remains for internal corruption. |
| F8 | The reverse Related existence check could note that it supports legacy reverse-owned records. | Claude review on PR #99 (comment 3) | Not-applicable | N/A — comment-only maintenance preference; the design and compatibility restart test already state this purpose. | Reject | N/A — no behavior change required. | The reverse check is intentional and regression-tested. |
| F9 | Related CLI add/remove output preserves caller order and uses `issue_id`/`related_issue_id` while list/MCP expose canonical `left_issue_id`/`right_issue_id`. | OMP eight-agent code review for PR #100 | Verified | Direct PR diff inspection reproduced by a regression fence that failed with `left_issue_id` observed as `Null` under the old output. | Accept | Review-fix Slice R2 emits canonical left/right endpoints for JSON and text and adds reversed-order mutation-result assertions. | Slice R2 gate PASS: the fence was RED under raw caller-order output and GREEN after restoration; final integration passed 1,109 tests with 1 ignored. |
| F10 | The route Result names obsolete pre-rebase SHAs, calls merged PR #99 a draft, and records stale verification totals. | OMP eight-agent code review for PR #100 | Verified | Direct comparison of `.rivets-2x2i/route.md` with the current PR topology and final local gate showed stale commit/PR state and a 1,106-test total. | Accept | Review-fix Slice R2 records merged PR #99, the rebased PR #100 adapter increment, canonical-result coverage, and the current gate total. | Direct record check PASS: no draft-PR claim remains and the route records 1,109 passing tests. |
| F11 | The tracker closes `rivets-2x2i` while all seven acceptance checkboxes remain unchecked. | OMP eight-agent code review for PR #100 | Verified | The changed JSONL record had `status:"closed"` and completion Notes but retained seven `[ ]` markers in its description. | Accept | Review-fix Slice R2 marks all seven completed criteria `[x]` without changing their text. | Direct record check PASS: seven checked and zero unchecked criteria remain. |

## Slice R2 checkpoint

- Affected unit tests: PASS — the real-process CLI fence passed with canonical reversed-order add/remove JSON and text.
- PENDING falsifiers: N/A — C7 was already `PASS`; Slice R2 adds a permanent review regression fence.
- Stress fixture: PASS — larger-first Related add, duplicate add, list, and larger-first remove all produced one canonical pair and successful removal.
- Implementation versus oracle: PASS — output matched independently sorted Issue IDs and literal `left_issue_id`/`right_issue_id` objects and phrases.
- Production-scale budget: N/A — no loop, scale-dependent work, or always-on phase was added.
- Regression fence: PASS — `related_and_discovery_cli_are_structured_symmetric_and_persistent`.
- Named mutation: PASS — restoring raw caller-order add output made the fence fail because `left_issue_id` was absent.
- Fence restored: PASS — canonical output restored and the focused fence passed.
- Final integration: PASS — `cargo fmt --check`; workspace clippy with warnings denied; 1,109 tests passed with 1 ignored.
