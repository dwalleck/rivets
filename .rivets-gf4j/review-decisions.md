# Review decisions: rivets-gf4j

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| F1 | The C5 Closed-prerequisite mutation left its fence green; diagnose the full blockedness path and strengthen or relocate the mutation before recording PASS. | advisor | Verified | With `find_blocked_issues` mutated to treat Closed prerequisites as active, `cargo test -p rivets --test in_memory_storage closed_prerequisite_stays_recorded_without_blocking` initially passed. Code reading showed `blocked_issues` has its own Closed filter while `ready_to_work` consumes `find_blocked_issues`. After the fence added literal assertions for both public paths, the same mutation turned red on Ready membership; restoration made the full `rivets` suite green. | Modify | Strengthened `closed_prerequisite_stays_recorded_without_blocking` to assert both `blocked_issues` and `ready_to_work` against the explicit Closed-state oracle; retained the graph-level named mutation because it now exercises its actual public consumer. | Accepted into uncommitted Slice 1; Slice 1 checkpoint passed, and later plan slices remain. |
| F2 | `BlockingDependency` derives `Deserialize`, which bypasses the self-reference invariant; deserialize through `new` or remove the unused trait. | DomainRelationshipReview | Verified | Code reading at `domain/relationship.rs:7-34` shows derived field construction bypasses the only equality check; the new self-reference JSON fence failed under derived deserialization and passed with validating deserialization. | Modify | Preserve the public serde contract with a custom validating `Deserialize` implementation and add valid/self-reference JSON fences; review-fix Slice 5. | Slice 5 gate PASS: literal constructor/JSON oracle agreed; derive mutation turned the fence red; restoration and the 1,097-test workspace gate were green; stress/loop/wall budgets N/A for a two-field value. |
| F3 | Blocking add/remove leave the dependent Issue's `updated_at` unchanged; set it to `Utc::now()` after successful mutation. | StorageImplTestsReview | Verified | A real CLI Workspace changed the dependent's persisted `dependencies` from empty to one Blocking record while `updated_at` remained byte-identical. | Reject | N/A — no accepted contract requires relationship mutations to change Issue metadata. | The retired generic add/remove implementation had the same timestamp behavior, and C0-C10 define relationship persistence/readiness without redefining `updated_at`; changing it here would add unapproved behavior rather than repair a regression. |
| F4 | The Closed-prerequisite fence proves in-memory blockedness but not that the edge remains recorded across MCP context recreation; add a persistence fence. | McpToolsTestsReview | Verified | `in_memory_storage.rs:517-575` checks retained in-memory queries; the original MCP close test checked blocked/ready only. The added recreated-context query stayed green normally and failed with zero retained edges when close was mutated to remove incoming Blocking edges. | Accept | Extended the MCP close/unblock test to recreate Tools context and assert the exact dependent/prerequisite pair remains listed; review-fix Slice 6. | Slice 6 C5 gate PASS: close-edge-removal mutation turned the new fence red; restoration and the final workspace gate were green. |
| F5 | MCP Blocking tests inspect Rust fields but not serialized output keys/envelopes; add wire-shape assertions. | McpToolsTestsReview | Verified | The original integration test directly inspected Rust fields while server tests covered input schemas only. Exact add/list/tree/remove `serde_json::Value` assertions failed when `prerequisite_id` was mutated to `depends_on_id`. | Modify | Added exact `serde_json::to_value` assertions for add/list/tree/remove results in the existing real-storage integration test; review-fix Slice 6. | Slice 6 C7 gate PASS: legacy-key mutation turned the wire fence red; restoration and the final workspace gate were green. |
| F6 | Current-reference docs still advertise removed `dep`, `--deps`, generic dependency-type validation, and generic storage APIs; migrate every stale reference. | TopDocsReview, FlowModuleDocsReview, ArchitectureDocsReview | Verified | Real CLI invocations returned exit 2 with `unrecognized subcommand 'dep'` and `unexpected argument '--deps'`; the cited README/data-flow/architecture/module-structure text published those forms. | Accept | Replaced the retired command, create flag, and public storage terminology with canonical Blocking Dependency forms across the cited current-reference docs; review-fix Slice 7. | Slice 7 gate PASS: canonical CLI/create help matched the docs, parity rendering passed, and the final workspace gate was green. |
| F7 | Wayfinder instructions require generic Parentage and Blocking mutations after the generic surface was removed; make the current limitation and supported Blocking operation explicit. | TopDocsReview | Verified | Router/CLI inspection found only dedicated Blocking operations and no Parentage mutation; verified Task `rivets-qcje` owns set/clear/move/show Parentage. | Modify (tracked at rivets-qcje) | Updated Wayfinder guidance to use canonical Blocking operations and label-based frontier membership, and stated that canonical Parentage is unavailable until `rivets-qcje`; review-fix Slice 7. | Restoring generic mutation would violate C8; the tracked Parentage slice remains the implementation owner. Documentation/parity and final workspace gates passed. |
| F8 | `docs/README.md` advertises 25 MCP tools while only 24 are registered; correct the count. | TopDocsReview | Verified | Structural count of `#[tool]` handlers in `server.rs` is 24, and the router parity test enumerates the same current set; the documentation said 25. | Accept | Changed the documented count to 24; review-fix Slice 7. | Parity registry and rendered-reference checks passed; final workspace gate passed. |
| F9 | `design.md` PENDING rows and its pre-cutover C0 log contradict later PASS results in `route.md`; rewrite the design proof statuses. | DesignRouteReview | Refuted | The workflow contract assigns `design.md` to falsifiable-design and uses PENDING to hand falsifiers to named build slices; `route.md:41-46` records later checkpoint/terminal evidence. The 2026-08-28 C0 entry explicitly records the pre-cutover seam probe, not the final registry proof. | Reject | N/A — historical stage artifacts retain their stage-local state; later gate evidence remains in the owning route/checkpoint records. | Rewriting design-time lifecycle state from assessing-review-feedback would violate artifact ownership and erase chronology; the apparent contradiction is stage progression. |
