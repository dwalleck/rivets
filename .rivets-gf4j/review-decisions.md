# Review decisions: rivets-gf4j

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| F1 | The C5 Closed-prerequisite mutation left its fence green; diagnose the full blockedness path and strengthen or relocate the mutation before recording PASS. | advisor | Verified | The first mutated run stayed green; code reading showed `blocked_issues` filters Closed independently while `ready_to_work` consumes `find_blocked_issues`. The strengthened fence then turned red under the mutation and green after restoration. | Modify | Strengthened `closed_prerequisite_stays_recorded_without_blocking` to assert both public paths against literal state expectations. | Accepted into Slice 1; checkpoint PASS. |
| F2 | Derived `Deserialize` bypasses `BlockingDependency::new`, allowing self-reference; route deserialization through the constructor and fence it. | Claude Code Review, PR #94 | Verified | Direct code reading confirmed derived construction bypassed `new`; `serde_json::from_value` with equal endpoint IDs succeeded before the fix. | Accept | Commit `ef8290a` adds serde `try_from` through a private raw record and a self-reference JSON regression fence. | Focused domain test, rivets Clippy, and final workspace gate PASS. |
| F3 | Mixed legacy migration fixtures are unrelated to the Blocking foundation and should be split out. | Claude Code Review, PR #94 | Verified | PR #94 contains the c5e5 fixture/helper and resilient-loader proof; later stack commits also carried its CLI fence because `rivets-gf4j` is blocked by closed prerequisite Task `rivets-c5e5`. | Modify | Retained the prerequisite migration proof in the dependency stack and commit `7b22a29` adds its missing tracing-to-stderr source fix so every stacked branch is independently green. | Diverges from removal: the tracked prerequisite was implemented but not committed before this stack; preserving it avoids a branch that cites a closed dependency while omitting its proof. |
| F4 | Hard 10/50 ms unit-test budgets may be flaky; ignore or move them to a benchmark. | Claude Code Review, PR #94 | Unverified | No flake reproduced: local repeated gates and PR #94 Linux/macOS/Windows CI all passed the 10k-Issue/50k-edge fixture. Missing evidence is an observed loaded-runner failure. | Reject | N/A — no verified failure and the approved plan requires a measured always-on budget. | Retaining the deterministic budget fence is the approved risk posture; revise only on observed CI instability. |
| F5 | New public trait methods may fail `missing_errors_doc` Clippy. | Claude Code Review, PR #94 | Refuted | Full all-target/all-feature Clippy and PR #94 Lint CI passed with `-D warnings`. | Reject | N/A — the claimed lint failure does not occur. | No behavior or documentation gap demonstrated by the configured gate. |
| F6 | `show` hides legacy non-Blocking relationships, causing silent visibility loss. | Claude Code Review, PR #95 | Verified | `execute_show` intentionally queries only typed Blocking prerequisites/dependents; raw legacy Related/Parentage/Discovery records remain persisted. | Reject (tracked at rivets-2x2i) | N/A — approved clean cutover removes generic presentation; typed Related/Discovery is tracked at `rivets-2x2i`, Parentage at `rivets-qcje`, and all-kind canonical persistence at `rivets-vio8`. | The final stack removes every generic route and current docs state the compatibility limitation; retaining mixed generic show output would violate C8. |
| F7 | The `Issue.dependencies` ordering comment is ambiguous beside `#[serde(skip)]`. | Claude Code Review, PR #95 | Verified | The field is skipped from domain JSON but still consumed by the separate JSONL record conversion. | Accept | PR #96 rewrites the aggregate comment to identify the field as JSONL compatibility representation only. | Documentation-only fix; final docs audit PASS. |
| F8 | MCP tree defaults to unlimited while CLI defaults to depth 5; confirm whether this is deliberate. | Claude Code Review, PR #95 | Not-applicable | This is an explicit adapter-default preference, not a factual defect; both adapters accept caller-supplied depth and define zero as unlimited. | Reject | N/A — adapter-specific omitted-argument mechanics are allowed while explicit depth semantics and results remain shared. | No risk acceptance or behavior change requested; the approved design records the MCP optional depth shape. |
| F9 | MCP tree response types appear after the models test module and should move for readability. | Claude Code Review, PR #95 | Not-applicable | Source placement does not change visibility, schema, or behavior. | Reject | N/A — naming/placement taste does not justify rewriting an already tested public model. | No maintenance defect or failed navigation/tooling check was demonstrated. |
| F10 | `BlockingDependencyError::SelfReference.issue_id` lacks the repository-conventional public field documentation. | Claude Code Review follow-up, PR #94 | Verified | Code reading confirmed the public variant field had no `///` field comment. | Accept | Commit `c84108c` documents the Issue carried by the self-reference error. | Documentation-only; focused Clippy and final workspace gate PASS. |
| F11 | The plan understates graph complexity as Blocking-edge-only even though petgraph enumerates all outgoing edge kinds before filtering. | Claude Code Review follow-up, PR #94 | Verified | `graph.edges(node).filter(Blocks)` scans total outgoing edges; the 50k mixed-edge fixture exercises that cost. | Accept | Commit `c84108c` corrects the plan to O(V + E_total) and retains the already-passing mixed-edge budget. | No behavior or approved budget changed; focused and final gates PASS. |
| F12 | Root `CLAUDE.md` still instructs agents to use the removed `dep` command. | Claude Code Review, PR #96 | Verified | The exact removed examples remained at `CLAUDE.md:78-89` while the parser absence fence rejects `dep`. | Accept | Replaced the section with canonical `blocking-dependency` add/list/tree examples and explicit endpoint roles. | Agent-documentation fix applied using the writing-for-agents guidance; final docs audit and workspace gate PASS. |
| F13 | `[Unreleased]` lacks an entry for the breaking generic dependency surface removal. | Claude Code Review, PR #96 | Verified | `CHANGELOG.md` listed smaller contract changes but no `dep`/MCP/storage removal. | Accept | Added one Changed entry naming the canonical replacement and prerequisite creation contract. | Documentation-only; final workspace gate PASS. |
| F14 | `IssueId::new(\"nonexistent\").clone()` redundantly clones an owned temporary. | Claude Code Review, PR #96 (repeated) | Verified | Direct code reading confirmed the clone has no semantic effect. | Accept | Removed the redundant clone from the missing-prerequisite test. | Mechanical cleanup; final workspace gate PASS. |
| F15 | Removing the generic test suite left reachable legacy ParentChild blockedness propagation without a fence. | Claude Code Review, PR #96 | Verified | `find_blocked_issues` still propagates through compatibility-loaded ParentChild edges, while no remaining test referenced `ParentChild`. | Modify | Added `legacy_parentage_propagation_remains_fenced_until_readiness_cutover`, seeding the legacy edge through import and asserting current `ready_to_work` propagation plus direct-only `blocked_issues`; canonical removal remains tracked at `rivets-brai`. | Diverges from asserting identical public paths because current production intentionally has that legacy asymmetry; focused and full gates PASS. |
| F16 | `docs/data-flow.md` still documents removed `create --deps` syntax. | Claude Code Review, PR #95 follow-up | Verified | The current-reference create flow still named `--deps` and type-prefixed IDs while the parser fence rejects that flag. | Accept | Replaced it with repeatable `--prerequisite` and all-or-nothing creation semantics. | Documentation-only; final docs audit and workspace gate PASS. |
| F17 | Canonical CLI/MCP Blocking operations lacked adapter-level missing-endpoint regression checks. | Claude Code Review, PR #95 follow-up | Verified | Storage missing-endpoint behavior was fenced, but the real CLI and MCP adapter tests did not exercise it. | Accept | Extended both real-process/context-recreation fences to require typed/non-zero missing-prerequisite errors and unchanged JSONL. | Behavior fence only; focused and full gates PASS. |
| F18 | Canonical Blocking tree text and empty-tree output lack CLI process coverage after deleting the old renderer tests. | Claude Code Review, PR #96 follow-up | Verified | The canonical CLI fence asserted only tree JSON; no test drove either text branch. | Accept | Extended the real CLI fence to assert the role-named tree header/edges and empty-tree message. | Focused and full workspace gates PASS. |
| F19 | CLI and MCP tests duplicate raw JSONL seeding for legacy Related records. | Claude Code Review, PR #96 follow-up | Verified | Both tests perform similar record splicing, but they live in separate integration crates with different lifecycle/context setup. | Reject | N/A — extracting a cross-crate helper would couple adapter-specific fixtures without changing observable coverage. | No production bug, failure, or third consumer justifies a new shared test seam. |
| F20 | Removing generic mutation temporarily removes creation of Related, Parentage, and Discovery relationships. | Claude Code Review, PR #96 follow-up | Verified | The final stack exposes only Blocking mutation while preserving legacy records. | Reject (tracked at rivets-2x2i) | N/A — this is the approved staged cutover; Related/Discovery are tracked at `rivets-2x2i` and Parentage at `rivets-qcje`. | The capability gap is documented and already covered by F6's clean-cutover decision. |
| F21 | Legacy Parentage can make an Issue absent from both Ready and Blocked; reconcile the two paths. | Claude Code Review, PR #96 follow-up | Verified | The F15 fence demonstrates `ready_to_work` transitive propagation while `blocked_issues` reports direct blockers only. | Reject (tracked at rivets-brai) | N/A — `rivets-brai` explicitly owns one canonical Blocked/Ready predicate and removal of Parentage effects. | Fixing it here would cross the approved Blocking-interface slice and duplicate tracked work. |
| F22 | Compatibility-only `DependencyType` still derives CLI `ValueEnum` after the generic parser is removed. | Claude Code Review, PR #96 follow-up | Verified | No remaining Clap argument consumes `DependencyType`; grep found the derive as the only CLI coupling. | Accept | Removed `ValueEnum` from the compatibility enum while retaining serde/display/from-string behavior for JSONL loading. | Compile-time cleanup; full workspace gate PASS. |
| F23 | C5's blockedness expectation was derived from storage state rather than pinned literally. | advisor | Verified | The test compared `blocked_issues` to `prerequisite_state.status != Closed`, allowing those observations to move together. | Accept | Replaced derived booleans with literal Open/Closed status, exact blocked row, and empty-after-close assertions while retaining Ready and relationship-history checks. | Complete `cargo test -p rivets` gate PASS. |
| F24 | The early `cargo test -p rivets blocking_dependency` filter did not select every new Slice 1 fence. | advisor | Verified | The filter omitted tests named `blocking_cycles_*`, `blocking_tree_*`, and `closed_prerequisite_*`. | Accept | Ran the complete `cargo test -p rivets` suite after the final C5 fence change; all 635 tests passed with 1 ignored. | The definitive full-workspace gates also covered all Slice 1 fences; no completion claim rests on the narrow command. |
| F25 | Blocking add/remove leave the dependent Issue's `updated_at` unchanged; set it to `Utc::now()` after successful mutation. | StorageImplTestsReview | Verified | A real CLI Workspace changed the persisted Blocking record while `updated_at` remained byte-identical. | Reject | N/A — no accepted contract requires relationship mutations to change Issue metadata. | The retired generic operations had the same behavior, and C0–C10 do not redefine `updated_at`; changing it here would add unapproved behavior rather than repair a regression. |
| F26 | The Closed-prerequisite MCP fence proves blockedness but not that the edge remains recorded after context recreation. | McpToolsTestsReview | Verified | The original MCP close test checked Blocked/Ready only. Under a mutation that removed incoming Blocking edges during close, the new recreated-context query returned zero relationships and failed. | Accept | Commit `5639410` recreates Tools context and asserts the exact dependent/prerequisite pair remains listed. | Close-edge-removal mutation red; restoration and the PR #96 1,053-test pre-commit gate green. |
| F27 | MCP Blocking tests inspect Rust fields but not serialized output keys/envelopes. | McpToolsTestsReview | Verified | Existing server tests covered input schemas only. Exact add/list/tree/remove values failed when serialized `prerequisite_id` was mutated to legacy `depends_on_id`. | Modify | Commit `5639410` adds exact `serde_json::Value` assertions using compact shared test helpers. | Direct serialization exercises the server's `Content::json` payload types without a second protocol harness; legacy-key mutation red and restored gate green. |
| F28 | Current-reference docs still advertise removed `dep`, `--deps`, generic dependency-type validation, and generic storage APIs. | TopDocsReview, FlowModuleDocsReview, ArchitectureDocsReview | Verified | Real CLI calls reject `dep` and `--deps`, while the cited README/data-flow/architecture/module-structure text still published those forms. | Accept | Commit `ccbd2df` replaces retired syntax and public storage terminology with canonical Blocking forms. | Documentation conflict preserved PR #96's stronger all-or-nothing creation wording; full pre-commit gate green. |
| F29 | Wayfinder instructions require generic Parentage and Blocking mutations after the generic surface was removed. | TopDocsReview | Verified | CLI/MCP expose only dedicated Blocking operations; verified Task `rivets-qcje` owns canonical Parentage. | Modify (tracked at rivets-qcje) | Commit `ccbd2df` uses canonical Blocking operations and label-based frontier membership, and identifies `rivets-qcje` as the Parentage owner. | Restoring generic mutation would violate C8; docs and full pre-commit gate green. |
| F30 | `docs/README.md` advertises 25 MCP tools while only 24 are registered. | TopDocsReview | Verified | Router enumeration contains 24 tools and its registry fence passes; the documentation said 25. | Accept | Commit `ccbd2df` changes the documented count to 24. | Registry and full pre-commit gates green. |
| F31 | `design.md` PENDING rows and its pre-cutover C0 log contradict later PASS results in `route.md`; rewrite the design proof statuses. | DesignRouteReview | Refuted | The workflow contract assigns design-time PENDING falsifiers to named build slices; `route.md` records later checkpoint evidence. The C0 entry explicitly records the pre-cutover seam probe. | Reject | N/A — historical stage artifacts retain stage-local state while later gate evidence remains in route/checkpoint records. | Rewriting design-time lifecycle state from assessing-review-feedback would violate artifact ownership and erase chronology. |
