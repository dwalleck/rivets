# Review decisions: rivets-gf4j

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| F1 | The C5 Closed-prerequisite mutation left its fence green; diagnose the full blockedness path and strengthen or relocate the mutation before recording PASS. | advisor | Verified | The first mutated run stayed green; code reading showed `blocked_issues` filters Closed independently while `ready_to_work` consumes `find_blocked_issues`. The strengthened fence then turned red under the mutation and green after restoration. | Modify | Strengthened `closed_prerequisite_stays_recorded_without_blocking` to assert both public paths against literal state expectations. | Accepted into Slice 1; checkpoint PASS. |
| F2 | Derived `Deserialize` bypasses `BlockingDependency::new`, allowing self-reference; route deserialization through the constructor and fence it. | Claude Code Review, PR #94 | Verified | Direct code reading confirmed derived construction bypassed `new`; `serde_json::from_value` with equal endpoint IDs succeeded before the fix. | Accept | Commit `ef8290a` adds serde `try_from` through a private raw record and a self-reference JSON regression fence. | Focused domain test, rivets Clippy, and final workspace gate PASS. |
| F3 | Mixed legacy migration fixtures are unrelated to the Blocking foundation and should be split out. | Claude Code Review, PR #94 | Verified | PR #94 contains the c5e5 fixture/helper and resilient-loader proof; later stack commits also carried its CLI fence because `rivets-gf4j` is blocked by closed prerequisite Task `rivets-c5e5`. | Modify | Retained the prerequisite migration proof in the dependency stack and commit `7b22a29` adds its missing tracing-to-stderr source fix so every stacked branch is independently green. | Diverges from removal: the tracked prerequisite was implemented but not committed before this stack; preserving it avoids a branch that cites a closed dependency while omitting its proof. |
| F4 | Hard 10/50 ms unit-test budgets may be flaky; ignore or move them to a benchmark. | Claude Code Review, PR #94 | Unverified | No flake reproduced: local repeated gates and PR #94 Linux/macOS/Windows CI all passed the 10k-Issue/50k-edge fixture. Missing evidence is an observed loaded-runner failure. | Reject | N/A — no verified failure and the approved plan requires a measured always-on budget. | Retaining the deterministic budget fence is the approved risk posture; revise only on observed CI instability. |
| F5 | New public trait methods may fail `missing_errors_doc` Clippy. | Claude Code Review, PR #94 | Refuted | Full all-target/all-feature Clippy and PR #94 Lint CI passed with `-D warnings`. | Reject | N/A — the claimed lint failure does not occur. | No behavior or documentation gap demonstrated by the configured gate. |
| F6 | `show` hides legacy non-Blocking relationships, causing silent visibility loss. | Claude Code Review, PR #95 | Verified | `execute_show` intentionally queries only typed Blocking prerequisites/dependents; raw legacy Related/Parentage/Discovery records remain persisted. | Reject (tracked at rivets-2x2i) | N/A — approved clean cutover removes generic presentation; typed Related/Discovery is tracked at `rivets-2x2i`, Parentage at `rivets-qcje`, and all-kind canonical persistence at `rivets-vio8`. | The final stack removes every generic route and current docs state the compatibility limitation; retaining mixed generic show output would violate C8. |
| F7 | The `Issue.dependencies` ordering comment is ambiguous beside `#[serde(skip)]`. | Claude Code Review, PR #95 | Verified | The field is skipped from domain JSON but still consumed by the separate JSONL record conversion. | Accept | PR #96 rewrites the aggregate comment to identify the field as JSONL compatibility representation only. | Documentation-only fix; final docs audit PASS. |
| F8 | MCP tree defaults to unlimited while CLI defaults to depth 5; confirm whether this is deliberate. | Claude Code Review, PR #95 | Not-applicable | This is an explicit adapter-default preference, not a factual defect; both adapters accept caller-supplied depth and define zero as unlimited. | Reject | N/A — adapter-specific omitted-argument mechanics are allowed while explicit depth semantics and results remain shared. | No risk acceptance or behavior change requested; the approved design records the MCP optional depth shape. |
| F9 | MCP tree response types appear after the models test module and should move for readability. | Claude Code Review, PR #95 | Not-applicable | Source placement does not change visibility, schema, or behavior. | Reject | N/A — naming/placement taste does not justify rewriting an already tested public model. | No maintenance defect or failed navigation/tooling check was demonstrated. |
| F10 | `BlockingDependencyError::SelfReference.issue_id` lacks the repository-conventional public field documentation. | Claude Code Review follow-up, PR #94 | Verified | Code reading confirmed the public variant field had no `///` field comment. | Accept | Commit `c84108c` documents the Issue carried by the self-reference error. | Documentation-only; focused Clippy and final workspace gate PASS. |
| F11 | The plan understates graph complexity as Blocking-edge-only even though petgraph enumerates all outgoing edge kinds before filtering. | Claude Code Review follow-up, PR #94 | Verified | `graph.edges(node).filter(Blocks)` scans total outgoing edges; the 50k mixed-edge fixture exercises that cost. | Accept | Commit `c84108c` corrects the plan to O(V + E_total) and retains the already-passing mixed-edge budget. | No behavior or approved budget changed; focused and final gates PASS. |
