# Review decisions: rivets-u2mz

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| F1 | Tools::info releases its storage-resolution guard before fetching cached information; concurrent explicit roots can evict that entry. Return the report under the same context guard. | ReportingReview | Verified | Original `information_survives_concurrent_cache_eviction` failed with WorkspaceNotInitialized under 64 concurrent valid roots; paired-guard fix passed. | Accept | Slice 1: shared-lock report fast path; on miss, initialize and clone information under one write guard. | Existing C2 snapshot contract preserved. Original RED/restored GREEN, all four reporting parity tests and all eight Slice 1 gates PASS; final 1,227-test workspace integration PASS. |
| F2 | Document that database_path is not canonicalized like Workspace/config identity. | PR #108 observations relayed by requester | Verified | reporting.rs preserves the backend path and the getter lacked an explicit canonicalization caveat; StorageConfig::to_backend rejects ParentDir components before backend construction. | Accept | Add the one-line symlink/canonicalization caveat to database_path(). | Documentation-only; the example involving configured `..` is not reachable through validated Workspace configuration. |
| F3 | Remove the stray blank line before the workspace_lock module export. | PR #108 observations relayed by requester | Not-applicable | N/A — cosmetic grouping preference; the blank line exists in lib.rs. | Accept | Remove the blank line; no exports or behavior change. | Nonbehavioral cleanup; no design/implementation gate required. |
| F4 | Process-based parity tests add test-runner overhead; no concrete optimization requested. | PR #108 observations relayed by requester | Verified | reporting_parity.rs makes six CLI cargo-run calls and starts one prebuilt MCP server per process-based test (three servers total); the fourth test runs 64 library tasks. Focused nextest: 4 passed in 1.332s; command 3.62s including 2.01s build. | Reject | N/A — retain current real-process behavioral fences. | No MCP restart per assertion. Measurement is one local warm-build run, not a claim about every CI runner; no evidence justifies a harness redesign in this documentation-only pass. |
| F5 | Unix-only symlink regression does not exercise Windows symlink behavior; no specific code fix proposed. | PR #108 observations relayed by requester | Verified | app_canonicalizes_symlink_root is cfg(unix); ci.yml:59 includes Ubuntu, macOS, Windows, and runs nextest. Portable reporting tests are not Unix-gated. | Reject | N/A — no Windows symlink fixture added in this documentation-only pass. | CI is not Linux-only. General path reporting runs in Windows CI; Windows-specific symlink behavior remains unverified by this fixture. This pass does not expand the platform-specific test harness or claim that coverage. |

## PR observation verification

Verification: `cargo fmt --check` PASS. `cargo nextest run -p rivets-mcp --test reporting_parity` PASS: 4 tests, 0 skipped; test phase 1.332s. F2/F3 alter comments/whitespace only; no behavior-changing review-fix slice or new tests are warranted. F4/F5 are recorded limitations, not deferred implementation promises.
